const notas = [5.5, 5.3, 7.7, 3.8]

for (let i in notas){
    console.log(`nota: ${notas[i]}`)
}

const pessoa = {
    nome:'Ana',
    Sobrenome: 'Costa',
    idade: 27,
    peso: 89
}

for(let atributo in pessoa) {
    console.log(`${atributo} = ${pessoa[atributo]}`)
}


// para comentar de forma rapida só apertar crtl + :;


